# logic/rename_config.py

class RenameConfig:
    """Configuration for building new file names."""

    date_format: str = "%y%m%d"
    index_padding: int = 3
    separator: str = "_"
    start_index: int = 1
